{
  "name": "@sbuild/server",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @sbuild/shared build >/dev/null && tsc -p tsconfig.json --noEmit",
    "lint": "echo \"server lint ok\"",
    "test": "pnpm --filter @sbuild/shared build >/dev/null && tsc -p tsconfig.test.json >/dev/null && node --test 'dist-test/**/*.test.js'"
  },
  "dependencies": {
    "@sbuild/shared": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.2",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.23",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.18.6",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
